# Development / CI tooling.
# Install alongside requirements.txt:
#     pip install -r requirements.txt -r requirements-dev.txt

-r requirements.txt

# Tests
pytest>=7.0,<10.0
pytest-cov>=4.0,<7.0

# Lint / security (same bounds as the CI workflow)
ruff>=0.4,<1.0
bandit>=1.7,<2.0
